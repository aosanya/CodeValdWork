# MVP Done — Completed Tasks

Completed tasks are removed from `mvp.md` and recorded here with their completion date.

| Task ID | Title | Completion Date | Branch | Notes |
|---------|-------|-----------------|--------|-------|
| MVP-WORK-001 | Library Scaffolding & Task Model | 2026-02-27 | — | `task.go`, `types.go`, `errors.go`; `TaskManager` interface + `taskManager` impl; status state machine in `types.go` |
| MVP-WORK-002 | ArangoDB Backend | 2026-02-27 | — | `storage/arangodb/arangodb.go`; `work_tasks` collection; all five CRUD + list operations; `arangodb_test.go` |
| MVP-WORK-003 | gRPC Service (TaskService) | 2026-02-27 | — | `proto/codevaldwork/v1/service.proto`; generated stubs in `gen/go/codevaldwork/v1/`; `internal/grpcserver/server.go` implements all five RPCs; `internal/grpcserver/errors.go` maps domain errors to gRPC codes |
| MVP-WORK-004 | CodeValdCross Registration | 2026-02-27 | — | `internal/registrar/registrar.go`; heartbeats to Cross on startup and every 10 s; sends `produces`, `consumes`, `addr`, and `routes` in each `RegisterRequest` |
| MVP-WORK-005 | Unit & Integration Tests | 2026-02-27 | — | `task_test.go` — comprehensive unit tests with `fakeBackend` covering CreateTask, GetTask, UpdateTask, DeleteTask, ListTasks, status-transition enforcement; `storage/arangodb/arangodb_test.go` — ArangoDB backend integration tests |
| MVP-WORK-006 | Service-Driven Route Registration | 2026-02-27 | — | Routes `POST /{agencyId}/tasks` (`create_task`) and `GET /{agencyId}/tasks` (`list_tasks`) declared in `registrar.go` with `GrpcMethod` + `PathBindings`; CodeValdCross dynamic proxy handles forwarding with no Cross code changes |
| MVP-WORK-007 | Migrate shared infrastructure to CodeValdSharedLib | 2026-03-02 | SHAREDLIB-009 | Replaced `internal/registrar/` with `SharedLib/registrar`; replaced `envOrDefault`/`parseDuration` helpers with `serverutil.EnvOrDefault`/`serverutil.ParseDurationString`; replaced gRPC server setup with `serverutil.NewGRPCServer()` + `serverutil.RunWithGracefulShutdown()`; replaced ArangoDB bootstrap with `arangoutil.Connect()`; replaced local `gen/go/codevaldcross/v1/` copy with SharedLib import; removed `internal/registrar/` package; updated `go.mod` |
| MVP-WORK-008 | Schema extension — `TaskGroup` + `Agent` TypeDefinitions; richer Task properties | 2026-04-28 | feature/WORK-008_schema_extension | Added `dueAt`, `tags`, `estimatedHours`, `context`, `completedAt`; `option` typing for `status`/`priority`; see `mvp-details/schema.md` |
| MVP-WORK-009 | Graph relationship API | 2026-04-28 | feature/WORK-009_graph_relationships | `work_relationships` edge collection, `work_graph` named graph, edge-label whitelist (`blocks`, `subtask_of`, `depends_on`, `member_of`, `assigned_to`); `CreateRelationship` / `DeleteRelationship` / `TraverseRelationships` on `TaskManager`; extracted fake `DataManager` |
| MVP-WORK-010 | Agent vertex + `assigned_to` edge | 2026-04-28 | feature/WORK-010_agent_assignment | Dropped `assigned_to` string property; added `UpsertAgent`, `AssignTask`, `UnassignTask` creating/removing the edge |
| MVP-WORK-011 | Hard blocker enforcement | 2026-04-28 | feature/WORK-011_blocker_enforcement | `pending → in_progress` traverses inbound `blocks`; new `ErrBlocked` (`FAILED_PRECONDITION`) with `BlockedByInfo` proto detail |
| MVP-WORK-012 | TaskGroup CRUD + `member_of` edge | 2026-04-28 | feature/WORK-012_task_group | TaskGroup CRUD wired through `TaskManager`; target-type whitelist enforced on `member_of` edges |
| MVP-WORK-013 | gRPC surface expansion | 2026-04-28 | feature/WORK-013_grpc_surface | 17 new RPCs (`AssignTask`, `CreateRelationship`, `DeleteRelationship`, traversal RPCs, `TaskGroup` CRUD, `UpsertAgent`); new proto messages; error mapping for `ErrBlocked` |
| MVP-WORK-014 | Pub/sub publishing pipeline | 2026-04-28 | feature/WORK-014_pubsub_pipeline | Typed payloads via SharedLib `eventbus.Publisher`; publish hooks for all six topics (`work.task.created`, `work.task.updated`, `work.task.status.changed`, `work.task.completed`, `work.task.assigned`, `work.relationship.created`); registrar `produces` list updated |
| MVP-WORK-015 | HTTP convenience routes | 2026-04-28 | feature/WORK-015_http_routes | 17 new routes for Task, TaskGroup, Agent, relationships declared in registrar `RegisterRequest` for Cross dynamic proxy |
| FEAT-20260602-001 | `WorkflowRun.name` — optional caller-supplied label, server-generated when omitted, unique per agency | 2026-06-02 | feature/Dev-WORK-FEAT-20260602-001_workflow_run_name | Added `name` property (schema + `UniqueKey`) and generator `pipeline-YYYY-MM-DD-HHMMSS-<6hex>`; new `ErrWorkflowRunNameExists` sentinel mapped to gRPC `ALREADY_EXISTS`; new `CreateWorkflowRun` gRPC RPC + `POST /work/{agencyId}/workflow-runs` registrar route (unblocks `start-pipeline` in CodeValdFunctions); `name` filter on `ListWorkflowRunsRequest`; new `GetWorkflowRunByName` lookup; whitespace-trim validation; full manager + server test coverage |
| FEAT-20260602-003 | `WorkflowRun` status state machine — Work is authoritative writer; `pending → in_progress` on `work.task.assigned`, `in_progress → failed` on any failure topic, `in_progress → completed` on `terminal_event` condition match | 2026-06-02 | feature/Dev-WORK-003_workflow-run-status-state-machine | `schema.go`: `terminal_event` property on WorkflowRun. `models.go`: `TerminalEvent` field + `CanTransitionTo` method on `WorkflowRunStatus`. `errors.go`: `ErrInvalidRunStatusTransition`. `events.go`: 4 new `work.run.*` topic constants + typed payloads; `ConsumedTopics` expanded with all failure + completion topics. `task.go`: `UpdateWorkflowRunStatus` on `TaskManager` interface. `workflow_run.go`: `UpdateWorkflowRunStatus` impl (validates transition, sets `completed_at` for terminal states, emits `work.run.*` event). `internal/server/run_status_handler.go`: `RunStatusHandler` with `HandleEvent`, `matchesTerminalEvent` (`topic:field=value:...` format), `extractRunContext`, `extractReason`. `internal/server/event_dispatcher.go`: `runStatus *RunStatusHandler` field; `HandleEvent` called for every ACKed event. `internal/config/config.go`: default subscribe topics expanded. 7 `RunStatusHandler` unit tests + 7 `UpdateWorkflowRunStatus` manager tests; `go build` ✅ `go vet` ✅ `go test -race ./...` ✅ |
| FEAT-20260602-002 | `workflow_run_id` on Task / TaskTodo + every `work.*` event payload (Work sibling of [Cross umbrella](../../../CodeValdCross/documentation/3-SofwareDevelopment/mvp-details/FEAT-20260602-001_workflow_run_id_propagation_umbrella.md)) | 2026-06-02 | feature/Dev-WORK-FEAT-20260602-002_workflow_run_id_in_work | `schema.go`: `workflow_run_id` property on Task + TaskTodo. `models.go`: `Task.WorkflowRunID`, `TaskTodo.WorkflowRunID`, `TaskFilter.WorkflowRunID`. `task_impl_converters.go`: round-trip property. `task_impl_task.go`: `CreateTask` writes the `started_task` edge when `WorkflowRunID` is non-empty; `ListTasks` filters on the property; `UpdateTask` event payloads carry it. `assignment.go`: `AssignTask` gains a `workflowRunID` param — empty preserves stored, equal is no-op, mismatch returns new `ErrWorkflowRunMismatch` (→ gRPC FailedPrecondition), empty stored is back-filled and `started_task` edge written. `todo.go`: `DispatchTaskTodo` payload carries it. `events.go`: `WorkflowRunID` added to `TaskCreated/Updated/StatusChanged/Completed/Failed/Assigned`, `TodoDispatched`, `TodoCompleted`. `proto/codevaldwork.proto`: `Task.workflow_run_id` (21), `TaskTodo.workflow_run_id` (17), `TaskFilter.workflow_run_id` (4); `service.proto`: `AssignTaskRequest.workflow_run_id` (6); `make proto`. `internal/server/server.go` + `agent_server.go` + `workflow_run_server.go`: converters propagate the field. `internal/server/event_dispatcher.go`: `aiTodoCreatedPayload`/`aiTaskPayload` gain `workflow_run_id`; `handleAITodoCreated` inherits run-id from parent Task (or explicit payload value) onto new TaskTodos; `updateTodoStatus` publishes `work.todo.completed` with run-id; `ai.task.failed`→`work.task.failed` bridge carries run-id. `internal/server/errors.go`: `ErrWorkflowRunMismatch` → FailedPrecondition. 8 new manager-level unit tests + schema test extension; `go build` ✅, `go vet` ✅, `go test -race ./...` ✅. |
| FEAT-20260602-004 | Rollback / compensation semantics — `POST /workflow-runs/{id}/rollback` coordinator + CodeValdWork artifact cleanup | 2026-06-02 | feature/Dev-WORK-FEAT-20260602-004_rollback-compensation-semantics | `models.go`: `WorkflowRunStatusRollingBack` (transient) + `WorkflowRunStatusRollbackFailed`; `CanTransitionTo` extended: failed/completed→rolling_back→rolled_back|rollback_failed; rollback_failed→rolling_back. `errors.go`: `ErrRollbackConflict` (double-rollback), `ErrForeignRunDependency` (409 cross-run dep). `events.go`: `TopicRunRollingBack`, `TopicRunRollbackFailed`, `TopicTaskRolledBack` + payloads; `AllTopics` updated. `task.go`: `RollbackWorkflowRun` + `DeleteWorkflowRunArtifacts` on `TaskManager` interface. `workflow_run_rollback.go`: `RollbackWorkflowRun` orchestrates rolling_back→cross-service stubs→`DeleteWorkflowRunArtifacts`→rolled_back|rollback_failed; `DeleteWorkflowRunArtifacts` hard-deletes Tasks+TaskTodos+incident edges, guards foreign-run deps, emits `work.task.rolled_back` per deleted Task. proto: `WORKFLOW_RUN_STATUS_ROLLING_BACK=6`, `WORKFLOW_RUN_STATUS_ROLLBACK_FAILED=7`; `RollbackWorkflowRun` RPC; gen/go regenerated. `internal/server`: `RollbackWorkflowRun` handler; new status cases in `workflowRunStatusToProto`; `ErrRollbackConflict`→FailedPrecondition, `ErrForeignRunDependency`→Aborted. 8 new unit tests. `go build` ✅ `go vet` ✅ `go test -race ./...` ✅ |
| FEAT-20260602-007 | `WorkflowRun` failure-pipeline budget gate — schema, RPC, HTTP route | 2026-06-02 | feature/Dev-WORK-FEAT20260602-007_workflow-run-failure-budget | `schema.go`: 5 new WorkflowRun properties (`parent_workflow_run_id`, `root_workflow_run_id`, `failure_pipeline_budget`, `failure_pipelines_used`, `counted_child_run_ids`). `models.go`: matching `WorkflowRun` struct fields. `workflow_run.go`: round-trip through `workflowRunToProperties` / `workflowRunFromEntity`; `intProp` helper for typed int round-trip. New `workflow_run_failure_budget.go` with 3 manager methods (`SetFailureBudget`, `IncrementFailureBudget`, `CreateRecoveryWorkflowRun`) split out to keep `workflow_run.go` from growing further. `errors.go`: `ErrFailureBudgetAlreadySet`, `ErrNotRootWorkflowRun`. `task.go`: 3 new interface methods. proto: 4 new WorkflowRun fields; 3 new `CreateWorkflowRunRequest` fields (`parent_workflow_run_id`, `root_workflow_run_id`, `failure_pipeline_budget`); new `IncrementFailureBudget` RPC + messages. `internal/server/workflow_run_server.go`: routes `CreateWorkflowRun` to recovery vs top-level path; `IncrementFailureBudget` handler; `workflowRunToProto` carries 4 new fields. `internal/server/errors.go`: new sentinels → FailedPrecondition. `internal/registrar/registrar.go`: new HTTP route `POST /work/{agency}/workflow-runs/{id}/increment-failure-budget`. 8 new unit tests cover round-trip, idempotency, exhaustion, non-root rejection, zero-budget-never-exhausts. `go build` ✅ `go vet` ✅ `go test -race ./...` ✅ |
